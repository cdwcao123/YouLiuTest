# 开发说明（NOTES.md）

## 1. 使用的 AI 工具

使用了 **Codex（OpenAI）** 作为 AI 辅助开发工具，配合人工审查完成全部代码。

具体使用方式：

- **需求梳理**：让 AI 先阅读 `README.md`、`TASKS.md`、`API_SPEC.md` 三个文档，输出任务清单和实现方案，我确认后再动手。
- **代码生成**：AI 生成了入库/出库/库存模块的实体、Repository、Service、Controller 以及 Vue 页面初稿。
- **测试与验证**：让 AI 为 Service 层和前端筛选逻辑生成单元测试，并辅助排查编译错误、运行冒烟测试。

### AI 帮我解决的一个具体问题

模板中的 `InboundOrderCreateRequest` 把明细类 `InboundItemRequest` 写成了包级顶层类（且为 `class`，不是 `public`），
而 Service 在 `com.wms.service` 包中无法引用。我让 AI 审查后，将明细类改为 `InboundOrderCreateRequest` 的
`public static` 嵌套类，同时补上 `@NotNull` 数量校验，编译问题随之解决。

### AI 生成代码中我发现并修复的问题

AI 第一版把 `OutboundOrderService` 设计成“先保存出库单、再扣减库存”。我审查后发现：库存不足时虽然事务会回滚，
但逻辑上应先完成库存校验/扣减再落单据，语义更清晰，也避免无谓地占用自增 ID。已调整为：
**先校验商品/库位 → 加锁扣减库存 → 保存出库单与明细**，并补充了“库存不足时不保存单据”的单元测试。

## 2. 任务完成情况

### 必做任务

**任务 1：入库单创建（已完成）**

- 后端 `POST /api/inbound-orders`：自动生成单号 `IN-YYYYMMDD-XXX`（当天序号三位补零），
  校验商品、库位存在性，使用 `@Transactional` 保证入库单、明细、库存更新在同一事务；
  库存累加使用 `INSERT ... ON DUPLICATE KEY UPDATE` 原子 SQL，并发入库不会丢失更新。
- 前端入库管理页：供应商名称 + 多行明细，商品远程搜索下拉、仓库 → 库位级联选择、数量输入、行删除、前端校验与提交。

**任务 2：库存查询（已完成）**

- 后端 `GET /api/inventory`：支持 `keyword`（商品名/SKU 模糊）、`warehouseId`、`locationCode`、`page/pageSize`；
  一次 JOIN 查出商品与仓库名称，避免 N+1；分页使用数据库分页而非全表加载。
- 前端库存页：300ms 防抖搜索、仓库/库位筛选、后端分页、库存 < 10 的行红色加粗高亮。

**任务 3：Bug 修复（已完成）**

1. **后端 Bug**：`ProductService.delete()` 原实现未校验关联数据。已改为删除前检查：
   - 商品是否存在关联库存 → 拒绝删除；
   - 商品是否出现在入库/出库单明细 → 拒绝删除。
2. **前端 Bug**：商品列表编辑后 `currentPage` 被强制重置为 1。已修复：
   - 编辑/新增提交后保留当前页码；
   - 同时把商品列表改为后端分页，删除当前页最后一条且非第 1 页时自动回退一页。

### 选做任务

**选做 A：出库单 + 库存扣减（已完成）**

- 后端 `POST /api/outbound-orders`，单号 `OUT-YYYYMMDD-XXX`。
- **并发控制方案**：对库存行使用悲观写锁（`SELECT ... FOR UPDATE`），锁住后校验库存充足再扣减。

  理由：
  - 出库是“读-改-写”操作，直接 `UPDATE ... WHERE quantity >= x` 虽可防超卖，但无法给用户明确的剩余库存提示；
  - 乐观锁（`version` 字段）在高冲突时会频繁重试，WMS 出库对同一库位同一商品冲突概率高；
  - 悲观锁在 MySQL InnoDB 下实现简单、语义明确，配合事务回滚能保证并发扣减串行执行、不超卖。
- 前端出库管理页：与入库页交互一致，调用出库 API。

**选做 B：单元测试（已完成）**

- 后端（Service 层，共 10 个用例，`mvn test` 通过）：
  - `InboundOrderServiceTest`：入库成功生成单号并累加库存、商品不存在、库位不存在、明细为空；
  - `OutboundOrderServiceTest`：出库成功扣减库存、库存不足拦截、无库存记录拦截；
  - `ProductServiceTest`：有关联库存/出入库单禁止删除、无关联可删除。
- 前端（筛选逻辑，共 5 个用例，`npm test` 通过）：
  - `buildInventoryQuery` 参数构造（去空格、空值省略、仓库/库位条件）；
  - `isLowStock` 低库存判断；
  - `debounce` 防抖只执行最后一次。

**选做 C：前端性能优化（已完成）**

- 库存列表已改为**后端分页**，不再一次加载全量数据；
- 关键词搜索增加 **300ms 防抖**；
- 商品列表同步改为后端分页。

## 3. 验证情况

- 后端：`mvn test` → BUILD SUCCESS，10 个用例全部通过。
- 前端：`npm run build` 通过；`npm test` 5 个用例全部通过。
- 端到端冒烟测试（连接本机 MySQL `wms` 库）：
  1. 商品分页列表正常（示例数据 5 条）；
  2. 创建入库单成功（`IN-20260808-001`），库位库存 150 → 160；
  3. 创建出库单成功（`OUT-20260808-001`），库存 160 → 155；
  4. 删除有关联库存的商品被后端拒绝（400）；
  5. 库存不足的出库请求被拒绝（400）。

## 4. 遇到的其它问题

- `vite.config.ts` 的 `/api` 代理默认指向 FastAPI 端口 8000，已改为 Java 后端端口 8080。
- 模板 `pom.xml` 中的注释为乱码（GBK/UTF-8 编码问题），不影响编译，未改动。

## 5. 如果还有更多时间

- 商品改为软删除（逻辑删除），保留历史单据可追溯性；
- 出入库单增加草稿/审核/完成的状态流转；
- 库存流水表（stock ledger），记录每次变动来源单号；
- 多实例部署时，出库用 Redis 分布式锁或数据库唯一约束 + 重试机制替代单库悲观锁；
- 为库存查询补充数据库索引与 EXPLAIN 验证。
